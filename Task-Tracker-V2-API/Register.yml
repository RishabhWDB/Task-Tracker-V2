info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/register
  body:
    type: json
    data: |-
      {
        "name": "Arya",
        "email": "arya@test.com",
        "password": "test123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
