info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/login
  body:
    type: json
    data: |-
      {
        "email": "arya@test.com",
        "password": "test123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
